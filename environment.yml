name: misid_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - xrootd=5.5.1
  - poetry
  - mamba
  - pip
  - pip:
  # pip-install packages on available in conda-forge or defaults
    - pidcalib2
